# 实训项目全过程管理平台 - 完整测试指南

## 环境准备

### 1. 确保 Docker Desktop 已启动

打开 Docker Desktop，等待状态变为 "Running"（绿色）。

验证命令：
```bash
docker ps
```

如果显示 `Cannot connect to the Docker daemon`，说明 Docker 还没启动完成，请等待。

---

## 快速启动（一键命令）

在项目根目录执行：

```bash
# Windows PowerShell
.\start.bat
```

或在终端中依次执行以下步骤：

---

## 手动启动步骤

### 步骤 1：启动数据库

```bash
cd docker
docker-compose up -d
```

这会启动：
- MySQL 8.0 (端口 3306)
- Redis 7 (端口 6379)
- MinIO (端口 9000/9001)

验证数据库是否启动：
```bash
docker ps
```

应该看到 `training-mysql`、`training-redis`、`training-minio` 三个容器在运行。

---

### 步骤 2：执行数据库迁移（建表）

```bash
cd apps/server
node scripts/migrate.js
```

预期输出：
```
🔄 Connecting to database...
✅ Database connected
✅ Migrations table ready
📁 Found 10 migration files
📝 Executing 001_users_and_auth.sql...
✅ 001_users_and_auth.sql executed successfully
...
🎉 All migrations completed!
```

---

### 步骤 3：导入测试数据

```bash
node scripts/seed.js
```

预期输出：
```
🔄 Connecting to database...
✅ Database connected
✅ Seeds table ready
📁 Found 1 seed files
🌱 Seeding 001_initial_data.sql...
✅ 001_initial_data.sql seeded successfully
🎉 All seeds completed!

📋 Test Accounts:
  Admin:    admin / admin123
  Teacher:  teacher001 / teacher123
  Student:  student001 / student123
```

---

### 步骤 4：启动后端服务

```bash
# 方式一：开发模式（热重载）
npm run dev

# 方式二：如果内存不足，使用 ts-node
npx ts-node --transpile-only src/server.ts
```

预期输出：
```
🔄 Testing database connection...
✅ Database connected successfully
🚀 Server is running on port 3000
📚 API Documentation: http://localhost:3000/api
```

---

### 步骤 5：启动前端服务

新开一个终端窗口：

```bash
cd apps/web
npm run dev
```

预期输出：
```
  VITE v5.4.21  ready in 1900 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

---

## 功能测试

### 测试 1：登录

1. 打开浏览器访问 http://localhost:5173
2. 使用以下账号登录：
   - **管理员**: `admin` / `admin123`
   - **教师**: `teacher001` / `teacher123`
   - **学生**: `student001` / `student123`

预期结果：登录成功后跳转到仪表盘页面。

---

### 测试 2：项目 CRUD

**创建项目**（需要教师/管理员权限）：
1. 以教师账号登录
2. 点击左侧菜单 "项目管理"
3. 点击 "创建项目" 按钮
4. 填写表单：
   - 项目名称：测试项目
   - 项目描述：这是一个测试项目
   - 开始日期：选择今天
   - 结束日期：选择一个月后
5. 点击提交

预期结果：项目创建成功，列表中显示新项目。

**查看项目详情**：
1. 点击项目列表中的项目名称
2. 查看项目详情页

**编辑项目**：
1. 在项目详情页点击 "编辑"
2. 修改项目名称
3. 保存

**删除项目**：
1. 在项目详情页点击 "删除"
2. 确认删除

---

### 测试 3：分组 CRUD

**创建分组**：
1. 点击左侧菜单 "我的分组"
2. 点击 "创建分组"
3. 选择项目、填写分组名称
4. 提交

**添加成员**：
1. 进入分组详情页
2. 点击 "添加成员"
3. 输入学生学号
4. 确认添加

**退出分组**：
1. 在分组详情页点击 "退出分组"

---

### 测试 4：进度提交

1. 点击左侧菜单 "进度管理"
2. 点击 "提交进度"
3. 选择阶段、填写标题和内容
4. 提交

---

### 测试 5：文档上传

1. 点击左侧菜单 "文档管理"
2. 点击 "上传文档"
3. 选择文件（支持 PDF、Word、图片等）
4. 填写文档描述
5. 上传

---

### 测试 6：评分（教师）

1. 以教师账号登录
2. 点击左侧菜单 "成绩管理"
3. 选择要评分的项目/学生
4. 填写各维度分数
5. 提交评分

---

### 测试 7：API 接口测试

使用浏览器或 Postman 测试后端 API：

**登录接口**：
```bash
curl -X POST http://localhost:3000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

预期返回：
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "user": { ... },
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "roles": ["admin"],
    "permissions": ["user:view", ...]
  }
}
```

**获取项目列表**（需要 Token）：
```bash
curl -X GET http://localhost:3000/api/v1/projects \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

---

## 常见问题排查

### 问题 1：数据库连接失败

**症状**：后端启动时报错 `ECONNREFUSED 127.0.0.1:3306`

**解决**：
1. 确认 Docker 已启动：`docker ps`
2. 确认 MySQL 容器在运行：`docker ps | grep training-mysql`
3. 检查 `.env` 配置是否正确
4. 尝试重启数据库：`docker-compose -f docker/docker-compose.yml restart`

---

### 问题 2：迁移脚本执行失败

**症状**：`migrate.js` 报错，提示表已存在或列不存在

**解决**：
1. 重置数据库：
   ```bash
   docker-compose -f docker/docker-compose.yml down -v
   docker-compose -f docker/docker-compose.yml up -d
   ```
2. 重新执行迁移

---

### 问题 3：前端请求 404

**症状**：浏览器控制台显示 API 请求 404

**解决**：
1. 确认后端已启动
2. 检查 `vite.config.ts` 中的 proxy 配置
3. 确认请求路径正确（不应有重复的 `/api`）

---

### 问题 4：登录后 Token 无效

**症状**：登录成功但后续请求 401

**解决**：
1. 检查浏览器 LocalStorage 中是否有 `auth-token`
2. 检查请求头中是否有 `Authorization: Bearer xxx`
3. 确认后端 JWT 密钥一致

---

## 测试数据说明

种子数据包含以下内容：

| 类型 | 数量 | 说明 |
|------|------|------|
| 管理员 | 1 | admin / admin123 |
| 教师 | 2 | teacher001, teacher002 |
| 学生 | 8 | student001 ~ student008 |
| 专业 | 4 | 计算机、软件工程、人工智能、数据科学 |
| 班级 | 5 | 各年级班级 |
| 实训题目 | 6 | 电商平台、图书管理、考试系统、AI机器人等 |
| 项目 | 4 | 2024春季实训项目 |
| 分组 | 5 | 各项目下的学生分组 |
| 进度记录 | 5+ | 各组进度提交 |
| 文档 | 5+ | 需求文档、设计文档等 |
| 公告 | 4 | 项目启动、进度提醒等 |
| 答辩安排 | 5 | 各组答辩时间地点 |
| 评分记录 | 5+ | 各维度评分 |
| 工作量记录 | 7+ | 学生工作量填报 |

---

## 性能测试

使用以下命令测试 API 性能：

```bash
# 安装 Apache Bench (Windows 可用 WSL 或安装 Apache)
ab -n 1000 -c 10 http://localhost:3000/api/v1/projects
```

---

## 停止服务

```bash
# 停止后端
Ctrl + C

# 停止前端
Ctrl + C

# 停止数据库
docker-compose -f docker/docker-compose.yml down

# 完全删除数据（包括数据库文件）
docker-compose -f docker/docker-compose.yml down -v
```

---

## 下一步功能建议

测试完成后，可以考虑添加以下功能：

1. **实时通知** - WebSocket 推送
2. **数据大屏** - ECharts 可视化
3. **在线讨论** - 小组内聊天
4. **文件预览** - PDF/Word 在线预览
5. **小程序对接** - 替换模拟数据为真实 API

---

**测试完成！** 🎉
