# 实训项目平台 - 添加测试数据指南

## 方法一：启动后端服务（推荐）

这是最完整的方法，可以体验系统的全部功能。

### 1. 启动数据库

```bash
# 进入项目目录
cd c:\Users\user\Documents\trae-soio\training-project-platform

# 启动 Docker 服务（MySQL + Redis + MinIO）
docker-compose up -d
```

### 2. 执行数据库迁移

```bash
# 进入后端目录
cd apps/server

# 安装依赖
npm install

# 执行数据库迁移（创建表结构）
npm run db:migrate

# 插入种子数据（角色、权限、测试用户等）
npm run db:seed
```

### 3. 启动后端服务

```bash
# 开发模式启动
npm run dev
```

后端启动后，访问 http://localhost:3000/api/health 检查是否正常运行。

### 4. 测试账号

| 账号 | 密码 | 角色 |
|------|------|------|
| admin | admin123 | 管理员 |
| teacher001 | teacher123 | 教师 |
| student001 | student123 | 学生 |
| student002 | student123 | 学生 |
| student003 | student123 | 学生 |

---

## 方法二：前端模拟数据（无需后端）

如果暂时不想启动后端，可以在前端代码中添加模拟数据。

### 修改 Dashboard.tsx 添加模拟数据

打开 `apps/web/src/pages/Dashboard.tsx`，修改 `fetchData` 函数：

```typescript
const fetchData = async () => {
  setLoading(true)
  try {
    // 模拟数据 - 无需后端
    const mockProjects: ProjectSummary[] = [
      { id: 1, name: '电商平台开发', status: 'in_progress', progress: 65, deadline: '2026-06-15' },
      { id: 2, name: '图书管理系统', status: 'pending', progress: 0, deadline: '2026-06-20' },
      { id: 3, name: '在线考试系统', status: 'submitted', progress: 100, deadline: '2026-05-30' },
    ]
    
    const mockGroups: GroupSummary[] = [
      { id: 1, name: '第一组', memberCount: 4, projectName: '电商平台开发' },
      { id: 2, name: '第二组', memberCount: 3, projectName: '图书管理系统' },
    ]

    setRecentProjects(mockProjects)
    setMyGroups(mockGroups)
    setStats({
      projectCount: 3,
      groupCount: 2,
      pendingTaskCount: 3,
      completedTaskCount: 8,
    })
  } catch (error) {
    console.error('获取仪表盘数据失败', error)
  } finally {
    setLoading(false)
  }
}
```

---

## 方法三：直接操作数据库

如果你需要添加特定的测试数据，可以直接执行 SQL。

### 1. 连接 MySQL

```bash
# 进入 MySQL 容器
docker exec -it training-mysql mysql -u root -p
# 密码: root123

# 选择数据库
USE training_platform;
```

### 2. 插入测试数据

```sql
-- 添加更多学生
INSERT INTO `users` (`username`, `password_hash`, `real_name`, `email`, `status`) VALUES
('student004', '$2b$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi', '刘同学', 'student4@example.com', 1),
('student005', '$2b$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi', '陈同学', 'student5@example.com', 1);

-- 添加项目
INSERT INTO `projects` (`name`, `description`, `teacher_id`, `status`, `start_date`, `end_date`) VALUES
('电商平台开发', '基于 Spring Boot + Vue 的电商平台', 2, 'active', '2026-05-01', '2026-06-30'),
('图书管理系统', '基于 Node.js 的图书管理', 2, 'active', '2026-05-01', '2026-06-30');

-- 添加分组
INSERT INTO `groups` (`name`, `project_id`, `leader_id`, `status`) VALUES
('第一组', 1, 3, 'approved'),
('第二组', 2, 4, 'approved');
```

---

## 快速启动命令汇总

```bash
# 1. 一键启动所有服务（在项目根目录）
docker-compose up -d
cd apps/server && npm install && npm run db:migrate && npm run db:seed && npm run dev

# 2. 前端开发（新开终端）
cd apps/web && npm install && npm run dev

# 3. 访问系统
# Web 端: http://localhost:5173
# 后端 API: http://localhost:3000
```

---

## 常见问题

**Q: 数据库连接失败？**
A: 检查 Docker 是否启动，以及 `.env` 文件中的数据库配置是否正确。

**Q: 登录提示密码错误？**
A: 种子数据中的密码是占位符，需要重新执行 `npm run db:seed` 或使用正确的 bcrypt 哈希值。

**Q: 如何重置数据库？**
A: 执行 `npm run db:reset` 可以清空并重新初始化数据库。
